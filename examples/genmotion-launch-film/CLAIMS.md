# Claim ledger

| Claim | Evidence allowed in picture |
| --- | --- |
| `One editable source of truth.` | Real Studio workflow and editor captures; the project Creative IR used for render. |
| `Direct. Inspect. Revise.` | Real Studio controls, preview, timeline, and workflow shown without reconstruction. |
| `TYPE PATHS CAMERA BLUR AUDIO` | Features present in this authored project and current Genmotion schema/runtime. |
| `ONE IR` | The checked-in `genmotion.json` composition. |
| `NATIVE PIXELS` | Native CPU renderer output and render-plan metadata. |
| `VERIFIED OUTPUT` | Strict validation, frame-boundary inspection, probe result, and encoded-video QA artifacts. |
