# Verified delivery

Create a deterministic render plan and resolve the delivery purpose. Validate every frozen dependency and reject conflicting rights. Render the native scene graph, mix audio, encode, fully decode, and accept the master only after its contract passes. Return the input/output attestation, probe, performance result, and inspectable review evidence.
